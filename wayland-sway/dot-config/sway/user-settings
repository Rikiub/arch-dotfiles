# Time to screen off (Default: 600)
set $swayidle_screen_off 600

# Time to active screenlock (Default: 1000)
set $swayidle_lock 1000

# Xwayland support (enable | disable)
xwayland enable

# --- AUTOSTART ---

# System
exec {
	nm-applet
	blueman-applet
}

# User
exec {
	gammastep-indicator
}
