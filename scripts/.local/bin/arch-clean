#!/usr/bin/env bash

# Remove $HOME/.cache
sudo echo "Erasing... $(du -sh $HOME/.cache/yay)" ;\
sudo rm -rf $HOME/.cache/yay ;\

# Clean pacman cache
paccache -r ;\

# Remove orphans
pacman -Qtdq | sudo pacman -Rns - ;\

# Remove optional/circular dependences
pacman -Qqd | sudo pacman -Rsu -\
