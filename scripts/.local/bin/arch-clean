#!/usr/bin/env bash

# remove .cache
sudo rm -r -f $HOME/.cache/yay ;\

# clear pacman cache
paccache -r ;\

# remove orphans
pacman -Qtdq | sudo pacman -Rns - ;\

# remove optional/circular dependences
pacman -Qqd | sudo pacman -Rsu -\
