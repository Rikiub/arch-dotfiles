#!/usr/bin/env bash

if [ $# -eq 0 ]; then
    echo "Usage: <path/to/img>"
    exit 1
fi

find "$1" \
	| while read -r img; do
		swww init
		swww img $img \
		--transition-step 5 --transition-duration 3 --transition-type top
	done
