#/usr/bin/env sh

# find and delete broken symlinks
find ~/.config -xtype l -delete
find ~/.local -xtype l -delete

# sync stow files
stow --dotfiles --dir=$HOME/dotfiles --target=$HOME \
default scripts fish sway-rice i3

echo "> Completed dotfiles sync"
